const Header = () => {
  return (
    <>
      <p className="text-muted">
        You can set up custom fields for items in inventory in your preferred format. E.g. (Printers
        - Brand, Model, Year, Price) <br></br>
        To create a new custom field add new elements, edit them, or drag them to delete box.
      </p>
    </>
  );
};

export default Header;
